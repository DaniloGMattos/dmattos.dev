import { styled } from '~/stitches.config';

export const Container = styled('div', {
  width: '979px',
  height: '707px',
  borderRadius: '10px',
  backgroundColor: '#fff',
  position: 'relative',
});
